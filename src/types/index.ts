export type colorSchemeType = "light" | "dark" | "system";

export type userType = {
  username: string;
  email: string;
  profilePic: string;
  id: number;
  authId: string;
  authProvider: string;
};
